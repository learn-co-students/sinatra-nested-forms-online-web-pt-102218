require './environment'

module FormsLab
  class App < Sinatra::Base

    # code other routes/actions here

    get '/' do
      erb :root
    end
    "<h1>Welcome to the Nested Forms Lab!</h1>\n\n<p>In this lab, you'll
    practice using nested forms in S... same name property. Capybara
needs some sort of unique indentifier, so we use id. </li>\n\n</ul>\n" to include "let's navigate to the '/new'"

  end
end
